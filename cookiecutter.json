{
  "lambda_name": "Test Service One",
  "application_name": "{{ cookiecutter.service_name|replace('-', ' ')|replace(' ', '')|replace('.', '')|trim() }}Application",
  "lambda_slug": "{{ cookiecutter.service_name.lower()|replace('-', ' ')|replace(' ', '-')|replace('.', '-')|trim() }}",
  "safe_name": "{{ cookiecutter.service_slug.replace('-','_') }}",
  "service_registry": "{{ cookiecutter.service_slug }}",
  "githuborg": "bullyrooks",
  "workflow_token": "GH_TOKEN",
}